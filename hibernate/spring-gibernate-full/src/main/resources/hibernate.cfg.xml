<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://0.0.0.0:5432/product_project</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">157456Lb</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">8</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgresPlusDialect</property>

        <!--    &lt;!&ndash; Disable the second-level cache  &ndash;&gt;-->
        <!--    <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>-->

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">false</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">create-drop</property>

        <property name="current_session_context_class">thread</property>

        <mapping class="ru.larionov.springgibernatefull.model.Product"/>
        <mapping class="ru.larionov.springgibernatefull.model.Order"/>
        <mapping class="ru.larionov.springgibernatefull.model.Consumer"/>

    </session-factory>

</hibernate-configuration>
